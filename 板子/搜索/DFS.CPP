#define 满足输出条件 1
#define 尝试方法数 1
#define 满足进一步搜索条件 1
#define 为进一步搜索所需要的状态打上标记 1
#define 恢复到打标记前的状态 1
int search(int t)
{
    if (满足输出条件)
    {
        //输出解;
    }
    else
    {
        for(int i = 1; i <= 尝试方法数; i++)
            if(满足进一步搜索条件)
            {
                为进一步搜索所需要的状态打上标记;
                search(t+1);
                恢复到打标记前的状态;//也就是说的{回溯一步}
            }
    }
}